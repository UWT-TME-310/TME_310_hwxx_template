[pytest]
# Default behavior: only run structural tests (unless --problem specified)
addopts = -m structure
markers =
    structure: Tests that validate notebook structure and organization
    content: Tests that validate response content and quality
    execution: Tests that execute notebook code and validate results