{
  "name": "Python + Jupyter (fast start)",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "features": {},
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter",
        "github.copilot",
        "github.copilot-chat",
        "ms-toolsai.datawrangler",
        "quarto.quarto"
      ],
      "settings": {
        "python.analysis.indexing": true,
        "jupyter.askForKernelRestart": false
      }
    }
  },
  "onCreateCommand": "pip install -U pip && if [ -f requirements.txt ]; then pip install -r requirements.txt; fi",
  "postCreateCommand": "",
  "postStartCommand": "",
  "updateContentCommand": "if [ -f requirements.txt ]; then pip install -r requirements.txt; fi"
}